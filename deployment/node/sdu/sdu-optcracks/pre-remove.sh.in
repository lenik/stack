#!/bin/sh
# master-file

# Pre-Remove script should always exit 0 to force the state.

lineconf -ktm "Restore altova authentication sites [1]" \
    /etc/hosts @altova.1

lineconf -tm "Restore altova authentication sites [2]" \
    /etc/hosts @altova.2

exit 0
