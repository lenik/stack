2010-12-13  lenik

	* configure.ac: don't report portability warnings.

2010-12-08  lenik

	* .: add itree.tex to svn ignores

2010-11-29  lenik

	* README: create empty README files to be filled in future.

2010-11-03  lenik

	* debian/changelog: switch to unstable.

2010-10-20  lenik

	* debian/changelog: Maverick redist.

2010-10-18  lenik

	* AUTHORS, COPYING, ChangeLog, INSTALL, Makefile.am, Makefile.in,
	  NEWS, README, VERSION.av, aclocal.m4, configure, configure.ac,
	  debian/README.Debian, debian/README.source, debian/changelog,
	  debian/compat, debian/control, debian/copyright, debian/docs,
	  debian/postinst, debian/prerm, debian/rules, glassfishv3.ans,
	  install-sh, libauto/coolauto.am, missing, post-install-2.sh,
	  post-install.sh, pre-remove.sh: set eol-style to native

2010-09-29  lenik

	* debian/changelog: add changelog

2010-09-23  lenik

	* .: set ignores.
	* .: ignore *.gz

2010-09-22  lenik

	* VERSION.av, configure, configure.ac, debian/control: /node
	  refactor.
	* Makefile.in, libauto/coolauto.am: auto updates.

2010-09-21  lenik

	* Makefile.in, libauto/coolauto.am: auto updates.
	* Makefile.am, Makefile.in, VERSION.av, configure, configure.ac,
	  post-remove.sh, pre-remove.sh: remove post-remove.

2010-09-19  lenik

	* debian: ignore substvars
	* debian/postinst, debian/postrm, debian/prerm,
	  debian/sdu-opt-glassfish3.substvars: exclude substvars.

2010-09-17  lenik

	* Makefile.am, Makefile.in, VERSION.av, configure, configure.ac,
	  debian/postrm, post-remove.sh, pre-remove.sh: remove glassfish
	  dir in postrm instead of prerm.
	* glassfishv3.ans: add missings
	* debian, debian/README.Debian, debian/README.source,
	  debian/compat, debian/control, debian/copyright, debian/docs,
	  debian/postinst, debian/prerm, debian/rules,
	  debian/sdu-opt-glassfish3.substvars: add debian control files.
	* libauto/coolauto.am: libauto updates.

2010-09-16  lenik

	* Makefile.in, VERSION.av, post-install.sh: switch to zip mode.
	* Makefile.am, Makefile.in, VERSION.av, configure, configure.ac,
	  libauto/subst.am, post-install.sh, pre-remove.sh: sudo only when
	  necessary.

	  pre-create install dst dir, so as to run under appserv user.

	  libauto update.

2010-09-09  lenik

	* Makefile.am, VERSION.av, configure, configure.ac,
	  post-install.sh: install glassfish by -unix.sh with answer file.

2010-09-08  lenik

	* Makefile.in, VERSION.av, configure, configure.ac, pre-remove.sh:
	  add remove messages.
	* Makefile.am, VERSION.av, configure, configure.ac,
	  post-install-2.sh, post-install.sh, pre-remove.sh: split
	  root-specific to -2.
	* VERSION.av, configure, configure.ac: package name changes.
	* VERSION.av, configure.ac: 0.1 release
	* Makefile.am, Makefile.in, VERSION.av, configure, configure.ac: am
	  fix.
	* VERSION.av, configure.ac, post-install.sh, pre-remove.sh:
	  initial.
	* ., COPYING, INSTALL, Makefile.in, VERSION.av, aclocal.m4,
	  configure, configure.ac, install-sh, libauto, missing: rename
	  glassfish to glassfish3
	* rename to sdu-opt-*
